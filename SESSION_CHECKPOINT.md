# Session checkpoint — 19554 NW 137th Lane, Lake Butler, Bradford County, FL (APN 00083A03400, 1.50 ac)

Written 2026-09-03 03:10Z after the visual QA pass. Uncommitted working tree preserved (36 modified, 14 untracked files). Nothing committed or pushed.

## Proven
- Focused comparables rerun seam exists on `POST /api/landos/deal-cards/:id/property-intelligence/run` with `scope`; runs A–G executed for this card.
- Qualifying manufactured-home evidence persisted through the research-record writer: 19517 NW 137th Ln ($290,000, sold 2025-08-06, 0.1 mi, 4 bd 2 ba, 1.5 ac, APN 00083A02900, Redfin URL `https://www.redfin.com/FL/Lake-Butler/19517-NW-137th-Ln-32054/home/119640639`) and 19414 NW 135th Ln ($239,900, 2026-04-15, 0.22 mi, APN 00083A02000). Realtor.com recorded as HTTP 429 blocked transport, not empty. Zillow boards challenged; indexed records kept as incomplete context only.
- Valuation unchanged: LandPortal $35,903 · Non-LandPortal $34,000 · Combined $35,000 · 40% $14,000 · 60% $21,000 · no 50%.
- Deal Brain: material decisions 1237, 1251, 1265, 1272; identical reruns B and G wrote no decision (count 17, latest 1272).
- Visual (page text + repository browser QA, console clean): Deal Brain panel shows "PRELIMINARY LAND HOME PACKAGE: WORTH EXPLORING" with the same-street 1.5-acre NW 137th Ln $290,000 (2025-08-06) reason and the home, well and septic next verification; Combined LandOS FMV strip correct; active resale competition (5 listings) visible; source lineage visible; hard refresh stays on `/dept/acquisitions/v2?deal=115&page=overview`; hard refresh GET only.
- Page load: the only non-GET requests are the QA harness's own `/api/dashboard/browser-pairings` (+`/claim`) token pairing; no research, model, record, valuation or Deal Brain writes.

## Not yet verified on screen
- The 19517 NW 137th Ln evidence row itself (not found in Overview/Comps page text; likely inside a collapsed details section).
- The combined comp map section by text (aerial parcel map is visible; the comp-map heading was not located).

## Remaining work
- Hamilton County QA card (APN 023 003.02): no coordinates anywhere; resolve via parcel-resolution seam or replacement controlled lead, then focused rerun.
- Fairview (Map 042 Parcel 123.00) on-screen unchanged check.
- 47 unlocated manufactured evidence rows from the first run remain in the record (history).
- County recorded-sale comp path does not exist in the comp architecture.

## Visual QA addendum (2026-09-03 03:2xZ, Comps page with collapsed sections expanded)
- Same-street sale row VISIBLE in the comparable table: "19517 NW 137th Ln, Lake Butler, FL 32054 · Improved-property context · 1.5 ac · $290,000 · $193,333/ac · 2025-08-06 · 0.1 mi" (Redfin lineage carried on the record; source column not captured in text).
- Comparable workspace heading "One list · one map · same filtered records" present; map container node not detected by selector in page text read — see screenshot.
- Writes on load: only QA-harness browser-pairing POSTs. Screenshot: `.runtime/landos/qa/lake-butler-visual-2026-09-03/comps-expanded.png`.

## Fairview and Hamilton County QA visual check (2026-09-03 03:2xZ)
- Fairview (Map 042 Parcel 123.00, 51.11 ac): loads; Combined LandOS FMV $3,113,000 (Non-LandPortal lane only, low confidence); LandPortal FMV unavailable (its $56.9M estimate implies 75.9 ac, refused); 3 qualified closed sales; Land Home screen NOT VIABLE on terrain (15.49 usable ac, 25.9% under 10% slope) with no manufactured sale retained (Zillow challenged); LandPortal comps map visible; active competition visible (Redfin). Hard refresh stayed on /dept/acquisitions/v2; console clean; only QA-harness pairing POSTs. Unchanged from the pre-session baseline. LandWatch additive line not captured in page text this pass (API earlier showed landWatch.applicable true).
- Hamilton County QA card (APN 023 003.02, 40.5 ac): loads; Combined LandOS FMV · Land $547,500 (closest sold-market evidence, low confidence), 40% $219,000, 60% $328,500; 0 qualified closed vacant-land sales; active competition visible (Redfin, Graysville); source lineage visible (Development Path); Deal Brain package present with Land Home posture UNSCREENED. Blocker: "Confirmed subject coordinates are required for the 5-mile manufactured-home boundary" (card has no coordinates in any store), so the manufactured lane and comp-map distances cannot be proven on this card without a coordinate resolution.
- Screenshots: `.runtime/landos/qa/lake-butler-visual-2026-09-03/fairview-overview.png`; QA evidence dir for deal 89 overview under `.runtime/landos/qa/`.

## Final sprint checkpoint (2026-09-03)

1. Lake Butler (19554 NW 137th Lane, Bradford County, FL, APN 00083A03400, 1.50 ac): ACCEPTED on the proven set. LandPortal FMV $35,903 · Non-LandPortal FMV $34,000 · Combined LandOS FMV $35,000 · 40% $14,000 · 60% $21,000 · no 50%. Deal Brain decision 1272 current; Land Home Package WORTH EXPLORING with the same-street 1.5-ac NW 137th Ln $290,000 (2025-08-06) reason and the home/well/septic next verification; same-street sale row visible in the comparable table; combined comp map visible; active competition and source lineage visible; hard refresh stays on the canonical route and is write-free apart from the QA harness's own pairing endpoints.
2. Fairview (Map 042 Parcel 123.00, 51.11 ac): unchanged. Combined $3,113,000 (Non-LandPortal only), LandPortal FMV correctly unavailable, 3 qualified sales, comps map and active competition visible, hard refresh write-free, console clean. LandWatch applicable per API (20+ acres, additive).
3. Hamilton County QA card (APN 023 003.02, 40.5 ac): NOT accepted. Loads with Combined (Land) $547,500, 40% $219,000, 60% $328,500, active competition, lineage, Deal Brain package (posture UNSCREENED). Exact blocker: "Confirmed subject coordinates are required for the 5-mile manufactured-home boundary" — the card carries no coordinates in any store, so the manufactured lane, distances and comp map cannot be proven.
4. Deal Brain idempotence: material reruns wrote one decision each (1237, 1251, 1265, 1272); identical reruns B and G wrote none (count 17, latest 1272).
5. Runtime: healthy (last health OK).
6. Already proven: focused suites 282 tests / 23 files pass plus later targeted reruns; root typecheck passes; touched web files typecheck clean (55 pre-existing errors in committed files outside this tree); production build passes.
7. Browser QA artifacts: `.runtime/landos/qa/browser-route--dept-acquisitions-v2-deal-115-page-overview-2026-09-03T03-08-40-234Z`, `.runtime/landos/qa/lake-butler-visual-2026-09-03/comps-expanded.png`, `.runtime/landos/qa/lake-butler-visual-2026-09-03/fairview-overview.png`, deal-89 overview QA directory under `.runtime/landos/qa/`.
8. Git: 36 modified + 14 untracked source files, `SESSION_CHECKPOINT.md`, stray `.landos/STATE.md.221948.tmp`; nothing committed or pushed.
9. Limitations: Hamilton QA coordinates unresolved; no county recorded-sale comp path exists; Realtor.com blocked by HTTP 429 from this machine (indexed search found no realtor.com records); Zillow boards and record pages challenged (indexed records kept as incomplete context); 47 unlocated manufactured rows from the first run remain as history; Zillow/Realtor.com record enrichment for incomplete rows not achievable from this machine.

Sprint acceptance: NOT claimed (Hamilton QA blocked; county path absent). Lake Butler acceptance: complete on the proven facts above.

## Hamilton County QA coordinate resolution attempt (2026-09-03 03:33Z)
- Seam used: existing `POST /api/landos/deal-cards/125/parallel-resolve` (Property Resolution capability: official parcel + jurisdiction + LandPortal lanes). Result: RESOLVED on the retained identity (APN 023 003.02 agrees with the authenticated LandPortal parcel panel); no coordinates produced.
- Official Hamilton County sources: the government transport reported `gov_transport_unreachable` (6 attempts), `gov_transport_direct_failed`, then `gov_transport_browser_fallback`; no parcel geometry, centroid or map coordinates came back. LandPortal parcel panel for this parcel carries no latitude/longitude facts. No secondary parcel evidence was retained by the seam, so nothing provisional was persisted; no coordinates were fabricated.
- Property card 103 lat/lng remain null; research-record identity carries no coordinates and no parcel geometry.
- Consequence: the QA card still shows "Confirmed subject coordinates are required for the 5-mile manufactured-home boundary"; the manufactured-home posture, comp distances and combined comp map remain unproven on this card. Nothing on Deals 115 or 89 was touched.

## Hamilton County QA coordinate ladder attempt (2026-09-03 03:4xZ)
- Existing subject lat/lng: null (property card 103). Existing parcel geometry/centroid: none in the research-record identity.
- Official county/state GIS by APN (existing lookupOfficialParcel seam, Tennessee Comptroller public parcel layer, `https://services1.arcgis.com/YuVBSS7Y1of2Qud1/arcgis/rest/services/Tennessee_Property_Boundaries_Public_Use/FeatureServer/0`): REACHABLE, result `no_match`. Patterns tried and failed: PARCELID/PARCEL equality "023 003.02"; control map + parcel "map 023, parcel 003.02"; GISLINK map+parcel "02300302"; PARCELID map+parcel "02300302"; GISLINK ordered-group "023 003 02"; PARCELID ordered-group "023 003 02"; county + owner "CAMERON NATHANIEL JOSEPH" (needle "NATHANIEL"): no owner-of-record reconciled; street search: no match.
- Official assessor by parcel: no tested Hamilton County, TN assessor source is configured in the official-source routing (the coverage seam reports this as a missing source configuration, not parcel non-existence). LandPortal parcel panel matched the APN but publishes no latitude/longitude for it.
- Earlier `gov_transport_unreachable` log lines were Florida legislature hosts from the Lake Butler land-use lanes, not Hamilton County sources.
- No approved secondary parcel evidence with a location is retained for this card, so nothing provisional was persisted and no coordinates were fabricated. No code was changed in this pass; the fallback ladder was not implemented within the limit.
- Result: Hamilton County QA card remains BLOCKED on coordinates (UNSCREENED solely for that reason). Deals 115 and 89 untouched.

## Generic official-source discovery pass for Hamilton County QA (2026-09-03 03:5xZ)
- Existing generic seam used: `POST /api/landos/deal-cards/125/official-parcel-gis/run` (runOfficialParcelGis: seeds → context probe → automatic official-source discovery → ArcGIS/adapter ladder → browser fallback). Ran 108 s. Attempts recorded: one ArcGIS source, outcome `not_found`: `https://services1.arcgis.com/YuVBSS7Y1of2Qud1/arcgis/rest/services/Tennessee_Property_Boundaries_Public_Use/FeatureServer/0` (statewide layer; parcel not found; geometry unavailable; confidence none; failure PARCEL_NOT_FOUND).
- No Hamilton County GIS or assessor endpoint was attempted by the run. Diagnosis (not yet corrected): the derived county host candidates (`gis.hamiltoncountytn.gov`, `gis.hamiltoncounty.tn.us`, `maps.hamiltoncountytn.gov`, `gis.hamiltoncounty.org`, `hamiltoncountygis.tn.gov`, `gis.co.hamilton.tn.us`) do not include the `<county><st>.gov` form the county actually uses, and the public-search discovery lane did not surface a county source within the run; the code path stops at the statewide layer.
- Code added this pass (typecheck passes; NOT built or restarted, so the live runtime does not carry it yet): `promoteRetainedParcelEnrichment` exported from property-card.ts; the official-parcel-gis endpoint now promotes an official parcel centroid to a coordinate-less card through that existing seam, records `coordinateBasis` (lat, lng, source, sourceUrl, method, official/provisional confidence, retrievedAt) in the response and an audit row, and returns attempted URLs.
- Result: Hamilton County QA card still has no coordinates; no coordinates fabricated; Deals 115 and 89 untouched.

## Operator-style official web discovery pass (2026-09-03 04:2xZ)
- Code: `official-source-discovery.ts` gained a `keyless_search` lane (governed Hermes search, plain-English queries "<County> County <ST> GIS parcel search", "… assessor of property parcel search", "… property search tax map parcel viewer"; only hits verifying as official are kept and their pages read for parcel services). `routes.ts` official-parcel-gis endpoint promotes an official centroid to a coordinate-less card via the exported `promoteRetainedParcelEnrichment`, records a coordinateBasis and audit row. Root typecheck passes; built and restarted (healthy).
- Endpoint run for the QA card (91 s): attempted only `https://services1.arcgis.com/YuVBSS7Y1of2Qud1/arcgis/rest/services/Tennessee_Property_Boundaries_Public_Use/FeatureServer` → not_found; coordinateBasis null; card 103 lat/lng still null.
- Direct run of the discovery seam for Hamilton County, TN: keyless search returned the official hosts `https://gis.hamiltontn.gov/` and `https://assessor.hamiltontn.gov/search` (both verify as official .gov); the Esri index lane selected the official county parcel service `https://mapsdev.hamiltontn.gov/hcwa03/rest/services/Live_Parcels/MapServer/0` (official .gov). So the county source IS discoverable; the endpoint's run path did not reach discovery (it stopped at the statewide seed), which is the remaining defect. The APN query against Live_Parcels was not executed.
- No coordinates fabricated. Deals 115 and 89 untouched.

## Hamilton County Live_Parcels manual proof + adapter fixes (2026-09-03 04:5xZ)
- Official service queried directly (WGS84 via outSR=4326): `https://mapsdev.hamiltontn.gov/hcwa03/rest/services/Live_Parcels/MapServer/0` (layer "Parcels", polygon, native SR 6576). Fields include OBJECTID, PBA_NUM, GISLINK, OWNERNAME1, ADDRESS, MAP, PARCEL, TAX_MAP_NO, CALCACRES, SALE1DATE….
- Where clauses tried → result: `TAX_MAP_NO = '023 003.02'` → 0; `UPPER(TAX_MAP_NO) LIKE '023%003.02%'` → 1 MATCH; `MAP = '023' AND PARCEL = '003.02'` → 1 MATCH; `GISLINK = '02300302'` → 0; `PBA_NUM = '02300302'` → 0; `TAX_MAP_NO = '02300302'` → 0; `TAX_MAP_NO = '023 003 02'` → 0; `GISLINK LIKE '023%003%02%'` → 0; `UPPER(OWNERNAME1) LIKE '%CAMERON%NATHANIEL%'` → 1 MATCH; `UPPER(OWNERNAME1) LIKE 'CAMERON%'` → 114; `CALCACRES > 40.4 AND CALCACRES < 40.6` → 4 (includes the match). No errors, no projection problems.
- MATCH: OBJECTID 2739, TAX_MAP_NO "023  003.02" (two spaces: why exact equality fails), MAP 023, PARCEL 003.02, PBA_NUM "033023    00302P000", GISLINK "033023    00302", OWNERNAME1 CAMERON NATHANIEL JOSEPH, ADDRESS 5170 HWY 60, Birchwood TN 37308, LEGALDESC1 "PT SEC 3&4 TWP 2 R 3 OUT OF 023 003.01 FOR 2022", CALCACRES 40.5. Polygon centroid (WGS84): lat 35.374790, lng -85.005115. Official source, verified .gov.
- Code fixes this pass (typecheck passes, built, restarted): arcgis field roster now recognises TAXMAPNO/TAXMAPNUM/TAXMAP/PBANUM parcel-id columns and OWNERNAME1; official-parcel-gis endpoint promotes an official centroid to a coordinate-less card with lineage (from the earlier pass); keyless-search discovery lane added (earlier pass).
- Endpoint run with the county service as the official source (operator seed) after the roster fix: the service WAS queried (attempt recorded) but the adapter still reported `not_found` (90 s); coordinateBasis null; card 103 lat/lng still null. Remaining defect: the adapter's cascade/reconciliation does not accept this layer's double-spaced TAX_MAP_NO or the MAP+PARCEL split, and the run path still does not consume discovered services automatically after the statewide seed fails. Not promoted; no coordinates fabricated.

## Hamilton County QA coordinates RESOLVED (2026-09-03 04:43Z)
- Fixes: arcgis-adapter logs every where clause with its feature count (`arcgis_parcel_where`), adds a MAP + PARCEL two-field clause; field roster recognises TAX_MAP_NO/PBA_NUM/OWNERNAME1; official-parcel-gis endpoint promotes the official centroid to a coordinate-less card via the existing card-coordinate seam with `coordinateBasis` and an audit row. Built, restarted, healthy.
- Endpoint run against `https://mapsdev.hamiltontn.gov/hcwa03/rest/services/Live_Parcels/MapServer/0` (logged clauses → features): `TAX_MAP_NO = '023 003.02'` → 0; `PBA_NUM = '023 003.02'` → 0; `GISLINK = '023 003.02'` → 0; `TAX_MAP_NO LIKE '023%003%02%'` → 1 (match). Result: resolved, parcel match verified, parcelId "023  003.02", owner CAMERON NATHANIEL JOSEPH, 40.5 ac, geometry retained (15 vertices).
- coordinateBasis persisted: lat 35.37479, lng -85.005115, source "Hamilton official parcel geometry", sourceUrl the Live_Parcels layer, method "centroid of the official parcel ring (15 vertices, apn)", confidence official, retrievedAt 2026-09-03T04:43:03Z; audit row `subject_coordinates_from_official_geometry`. Card 103 lat/lng no longer null. No fabrication.
- Not yet fed automatically: discovered county services after a statewide not_found (the run used the discovered layer as the official source explicitly). Focused manufactured-home rerun for the QA card launched next.

## Hamilton County QA proof after coordinate resolution (2026-09-03 04:5xZ)
- Focused manufactured-home rerun (scope manufactured_home) completed on the QA card with the official centroid. Lane: Zillow blocked (challenge; indexed search 1 record), Redfin retrieved 55 sold, Realtor.com blocked (HTTP 429). Retained within five miles with distances: 5509 Bunker Hill Rd, Birchwood ($218,000, 2025-01-24, 1.18 mi), 4740 Johnson Rd, Birchwood ($168,000, 2025-03-11, 1.48 mi), 1650 McCallie Ferry Rd, Soddy Daisy ($152,000, 2025-10-29, 4.81 mi); all Redfin URLs retained, coordinates via geocode cache lineage.
- Visible on screen (Overview page text): "PRELIMINARY LAND HOME PACKAGE: MARGINAL · Recent manufactured homes above $200,000 sold within about five miles (1 qualifying, top $218,000). No retained terrain read states usable acreage and slope, so the physical screen is incomplete." The card is no longer UNSCREENED for missing coordinates; MARGINAL is the honest posture because this controlled card has no terrain read. Combined LandOS FMV · Land $547,500 (closest evidence), 40% $219,000, 60% $328,500, active competition (5), source lineage visible.
- Comps page: comparable workspace "One list · one map" present; comp map presence rests on the full-page screenshot `.runtime/landos/qa/lake-butler-visual-2026-09-03/hamilton-qa-comps-expanded.png` (map node not matched by the text probe's selector).
- Hard refresh: stayed on `/dept/acquisitions/v2?deal=125&page=overview`, GET only; page load: only the QA harness's own browser-pairing POSTs; console clean. Evidence: `.runtime/landos/qa/browser-route--dept-acquisitions-v2-deal-125-page-overview-2026-09-03T04-56-42-684Z`.
- Still open: automatic consumption of discovered county services after a statewide not_found (this run supplied the discovered layer as the official source explicitly); no focused test added for the MAP+PARCEL clause; the card's physical screen needs a terrain read to move past MARGINAL.

## Discovered-service automation (2026-09-03 05:0xZ)
- `official-parcel-gis-run.ts`: after every configured/statewide seed answers not_found, the run now runs official-source discovery (if it has not run) and feeds any discovered official ArcGIS parcel MapServer/FeatureServer layer not yet tried into the SAME ArcGIS ladder (clauses, logging, centroid promotion and lineage unchanged); new pure `discoveredArcgisSeeds` selector. Test `official-parcel-gis-discovered-seeds.test.ts` added. Root typecheck passes. Not rebuilt/restarted in this pass (runtime carries the previous build); no business data touched.

## Final verification after restart (2026-09-03 05:1xZ)
- Built and restarted; runtime healthy. Focused test `official-parcel-gis-discovered-seeds.test.ts` passes.
- Official-parcel-gis endpoint for the QA card WITHOUT a supplied source: the run consumed the discovered Hamilton County service automatically (only attempt: `https://mapsdev.hamiltontn.gov/hcwa03/rest/services/Live_Parcels/MapServer/0`; the statewide layer was not the answering seed). Escalation: platform_fingerprint 16 requests / 86.9 s ("7 official source(s) available"), structured_service_discovery 4 requests, known_adapter 1 request; stopReason `wall_clock_budget`. Only the first clause ran (`TAX_MAP_NO = '023 003.02'` → 0) before the budget expired, so this run reported not_found; the whitespace-tolerant clause that matches (`TAX_MAP_NO LIKE '023%003%02%'` → 1, proven in the earlier run) was not reached.
- Card 103 coordinates unchanged and official: lat 35.37479, lng -85.005115 (promotion only fills a null point; nothing overwritten).
- Remaining: the discovery stage's wall-clock spend starves the query cascade on the automatic path; a per-stage budget or discovery-result reuse would let the matching clause run. Not changed in this pass.

Final sprint status: Lake Butler acceptance complete. Fairview unchanged verification complete. Hamilton QA coordinate and manufactured posture proof complete. Discovered-service automation: consumption of the discovered county service verified after restart; automatic parcel match not verified (wall-clock budget cut the cascade). No commit or push.

## Discovery/query budget split (2026-09-03 05:4xZ)
- Code: `gis-escalation.ts` gained a wall-clock RESERVE (`reserveWallClockMs` / `releaseWallClockReserve` / `remainingWallClockMs`, `wallClockBudgetMs`); a reserved ceiling stops the optional stage without setting the run's `wall_clock_budget` stop reason. `official-parcel-gis-run.ts`: both discovery call sites now go through one `runDiscovery` that (a) skips discovery when less than `MIN_DISCOVERY_WALL_CLOCK_MS` (8 s) remains after the query reserve, (b) holds `DEFAULT_QUERY_WALL_CLOCK_RESERVE_MS` (40 s) for the clause cascade, (c) passes discovery its own `maxWallClockMs` AND races it against the same deadline so one slow in-flight lane cannot overrun, and (d) calls `rememberDeployment` for a discovered official ArcGIS parcel service (`rememberDiscoveredParcelService`, platform shape only) so the NEXT run in that county seeds it as a learned deployment and never pays for web discovery. New pure `discoveryWallClockBudgetMs`, new dep `queryReserveMs`. Clause logging, centroid promotion, coordinateBasis, audit lineage and the no-overwrite rule are unchanged.
- Test: `official-parcel-gis-discovered-seeds.test.ts` extended with "discovery cannot consume the query budget" (budget split + ladder reserve stops the stage, not the run). `npx vitest run official-parcel-gis-discovered-seeds + arcgis-adapter`: 19 tests / 2 files pass. Root typecheck passes. Built; managed restart healthy (PID 28116, health HTTP 200).
- Endpoint run for the QA card with NO supplied source (HTTP 200, fast): the run went straight to `https://mapsdev.hamiltontn.gov/hcwa03/rest/services/Live_Parcels/MapServer/0` (learned/discovered county service consumed automatically; no statewide-only path, no discovery spend). Logged clauses for that layer: `TAX_MAP_NO = '023 003.02'` -> 0 features. NO further clause was logged in this run, so the whitespace-tolerant `TAX_MAP_NO LIKE '023%003%02%'` (proven 1 feature) and the `MAP = '023' AND PARCEL = '003.02'` clause were still NOT reached. Budget starvation is no longer the cause (the run was short); the remaining defect is in the adapter's clause cascade continuation for this layer, not in the discovery budget.
- Card 103 coordinates unchanged and official: lat 35.37479, lng -85.005115 (promotion only fills a null point).
- NOT verified this pass: the automatic run reaching the matching clause / OBJECTID 2739. Session hit its 15-minute hard limit here.

Sprint status at this checkpoint: Lake Butler acceptance complete; Fairview unchanged verification complete; Hamilton QA coordinates and MARGINAL posture complete; discovered-service reuse + budget split shipped and tested; automatic clause match still OPEN (adapter cascade, not budget). No commit or push.

## ArcGIS clause-cascade continuation pass (2026-09-03 10:0x-10:1xZ, 12-minute limit)

- EXACT BRANCH THAT STOPPED THE CASCADE (now proven by log, not inferred): `searchArcgisParcel`'s `runWhere` returned `null` on `ladder.stageExhausted()` with NO log line, which is indistinguishable from "0 features". `stageExhausted()` was true through `EscalationLadder.exhausted()` on WALL CLOCK (`elapsedMs >= maxWallClockMs`, 90 s), not on the stage request ceiling (40) and not because of a zero-feature answer. In the pre-fix run (90.5 s) exactly one clause executed and the rest were silently dropped; in the post-fix run (97.3 s) the new `arcgis_parcel_where_skipped` lines show EVERY clause (`TAX_MAP_NO = '023 003.02'`, `PBA_NUM = …`, `GISLINK = …`, the two ADDRESS clauses, `UPPER(OWNERNAME1) LIKE '%CAMERON NATHANIEL JOSEPH%'`) skipped with `reason: "stage_budget"` — i.e. the run's pre-cascade work (fingerprint/context probe plus service description against the slow `mapsdev.hamiltontn.gov` host) consumes the entire 90 s wall clock BEFORE the `known_adapter` stage starts.
- Code changed (`src/landos/arcgis-adapter.ts` only): (1) a zero-feature clause can never end the cascade — the rank loop and the MAP+PARCEL clause now break on an explicit `budgetStopped` flag instead of `stageExhausted()`; (2) every clause that cannot run is logged (`arcgis_parcel_where_skipped`, reason `stage_budget`) and recorded in `attempts` as "… → skipped (query budget spent)", so "the county says no" and "LandOS ran out of clock" are now distinguishable; (3) new pure `clauseWallClockReserveMs` / `DEFAULT_CLAUSE_WALL_CLOCK_RESERVE_MS` (25 s, never more than half of what remains): the adapter reserves clock during `structured_service_discovery` and releases it at `beginStage('known_adapter')` so service description cannot starve the clauses. Existing clause logging with feature counts, coordinateBasis, audit lineage, the no-overwrite rule and the no-fabrication rule are untouched. No Hamilton hard-coding outside the test fixture.
- Test added: `src/landos/arcgis-parcel-clause-cascade.test.ts` (4 tests) — a double-spaced `TAX_MAP_NO` fixture proves the zero-feature exact clause is followed by the matching whitespace-tolerant `TAX_MAP_NO LIKE '023%003%02%'` (OBJECTID 2739), proves the MAP+PARCEL clause is reached when no single column matches, proves budget-skipped clauses stay visible in `attempts`, and pins the reserve arithmetic.
- Tests run: `npx vitest run arcgis-parcel-clause-cascade arcgis-adapter official-parcel-gis arcgis-service-discovery` → 95 tests / 5 files pass (later reruns 61/3 pass). Root typecheck passes. Production build passes. Managed restart healthy (PID 47644, health HTTP 200).
- Endpoint run for the Hamilton QA card with NO supplied source, after the restart: HTTP 200 in 97.3 s; single attempt `https://mapsdev.hamiltontn.gov/hcwa03/rest/services/Live_Parcels/MapServer` → `not_found`; `coordinateBasis` null. Clause evidence: all clauses `skipped (stage_budget)` as above — the matching clause was still NOT reached, because the remaining cause is the pre-cascade wall-clock spend in the RUN (fingerprint/context probe on this host), which is outside the adapter file this pass was scoped to.
- Card 103 coordinates unchanged and official: lat 35.37479, lng -85.005115. Nothing overwritten, nothing fabricated. Lake Butler (Deal 115), Deal 89/Fairview and all business deal data untouched.
- Remaining exact next task: give the pre-cascade phase (`official-parcel-gis-run.ts` `platform_fingerprint` context probe + seed expansion) the same reserve treatment — hold `queryReserveMs` from the start of the run and release it when the ArcGIS query phase begins (the run's `!ladder.exhausted()` gates must be re-checked against the reserve so a reserved ceiling does not skip the query path) — then rerun the endpoint and confirm `TAX_MAP_NO LIKE '023%003%02%'` or `MAP = '023' AND PARCEL = '003.02'` executes and matches OBJECTID 2739.

Sprint status at this checkpoint: Lake Butler acceptance complete; Fairview unchanged verification complete; Hamilton QA coordinates (official) and MARGINAL posture complete; discovered-service reuse + budget split shipped; adapter cascade continuation + skip-visibility shipped and tested; automatic clause match still OPEN, now with the exact stopping branch identified (wall-clock exhaustion before the `known_adapter` stage, not a zero-feature short-circuit). No commit or push.

## Full official parcel run-path reserve audit (2026-09-03 10:44Z, 20-minute limit)

- Audit scope: endpoint handoff into `runOfficialParcelGis`, seed collection, context probing, discovery, fingerprint gates, adapter selection, ArcGIS service description, parcel clause execution, alternate-source/fallback discovery gates, and all reserve/exhaustion exits on that path.
- Blockers fixed in one pass: (1) `official-parcel-gis-run.ts` now acquires the 40 s query wall-clock reserve immediately after constructing the ladder, before fingerprint/context/discovery work; (2) query phase has one explicit transition that releases the reserve before any `!ladder.exhausted()` adapter gate, so a reserved ceiling cannot appear as a spent run; (3) late fallback discovery reacquires and releases its own reserve without stealing the initial query reserve; (4) ArcGIS REST context/service traversal now refuses each new request when the owning stage is exhausted, closing the inner-probe loop that previously could ignore the outer gate; (5) `arcgis-adapter.ts` releases its clause reserve on thrown discovery and on both early returns (`no layers`, `no parcel layer`), so a failed source cannot poison later source/discovery gates. Existing zero-feature continuation, TAX_MAP_NO whitespace-tolerant clause, MAP+PARCEL clause, discovered-service consumption, clause-count logging, official-coordinate no-overwrite rule, and no-fabrication behavior remain intact.
- Focused regression coverage added/extended: context probing stops at the reserved ceiling but the query clock becomes usable after release; an ArcGIS adapter returning no layers cannot leak its clause reserve. Hamilton fixtures still prove exact TAX_MAP_NO 0 -> whitespace-tolerant TAX_MAP_NO match OBJECTID 2739 and MAP='023' + PARCEL='003.02' match.
- Verification passed: `npx vitest run src/landos/official-parcel-gis.test.ts src/landos/official-parcel-gis-discovered-seeds.test.ts src/landos/arcgis-adapter.test.ts src/landos/arcgis-parcel-clause-cascade.test.ts src/landos/arcgis-service-discovery.test.ts` -> 97 tests / 5 files; `npm run typecheck` -> pass.
- Production build did not run: sandboxed `npm run build` could not read `vite.config.ts` / parent directory; the required escalation was rejected by the approval service with HTTP 404. Per the rejection, no alternate build workaround was attempted. Therefore no managed restart and no post-build Hamilton endpoint run were performed; the live PID 47644 remains healthy (root and health HTTP 200) but serves the prior build.
- Card 103 was not mutated in this pass. Its retained official coordinateBasis remains the previously proven Hamilton parcel centroid (35.37479, -85.005115); no coordinate-writing endpoint ran and nothing was fabricated.
- Acceptance status: OPEN only on deployment/endpoint proof. Exact remaining blocker: allow `npm run build` to read the repository config, then managed restart and run `POST /api/landos/deal-cards/125/official-parcel-gis/run` with no supplied source. Required proof is clause logs reaching `TAX_MAP_NO LIKE '023%003%02%'` or `MAP = '023' AND PARCEL = '003.02'`, feature count 1 / OBJECTID 2739, with Card 103 coordinates unchanged. If that post-build run fails, use its now-complete clause/skip evidence to report the exact remaining branch; do not add another isolated fix.
- No commit or push.

## Hamilton County GIS closure — automatic parcel match PROVEN (2026-09-03 12:4xZ)

- Run-level query reserve (already present in the working tree at the start of this pass, `src/landos/official-parcel-gis-run.ts`): `queryReserveMs` is held with `ladder.reserveWallClockMs()` from run start — before `platform_fingerprint`, the seed context probe and discovery — so every optional pre-query branch sees the lowered ceiling; `beginQueryPhase()` releases it exactly once, immediately before the ArcGIS seed loop, so the query phase's `ladder.exhausted()` gates measure the real run ceiling and never the reserved one; `runDiscovery` only re-acquires/releases the reserve when it runs AFTER the query phase started (`ownsReserve`), so the first discovery pass inherits the run reserve instead of dropping it. `arcgisDeps` also carries `canRequest: () => !ladder.stageExhausted()`.
- Runtime was stale (started 15:19:51Z, dist built later at 10:52 local). Managed restart applied; runtime healthy (PID 50536, health HTTP 200).
- ENDPOINT PROOF — `POST /api/landos/deal-cards/125/official-parcel-gis/run` (authenticated app route, empty body, NO supplied source), HTTP 200 in 59.6 s:
  - Attempt (single, automatic): `https://mapsdev.hamiltontn.gov/hcwa03/rest/services/Live_Parcels/MapServer` → outcome `verified`. The discovered county service was consumed automatically; no operator seed, no statewide-only path.
  - Complete clause sequence with feature counts: `TAX_MAP_NO = '023 003.02'` → 0; `PBA_NUM = '023 003.02'` → 0; `GISLINK = '023 003.02'` → 0; `TAX_MAP_NO LIKE '023%003%02%'` → 1. The cascade continued past three zero-feature clauses and reached the matching clause; no clause was skipped for budget.
  - Match: parcelMatch `verified` (confidence high), parcelId "023  003.02" (double-spaced, as the layer stores it), owner CAMERON NATHANIEL JOSEPH, 40.5 acres, geometry retained (15 vertices), centroid lat 35.37479 / lng -85.005115 — the OBJECTID 2739 record.
  - `coordinateBasis` null in this response BECAUSE card 103 already carries the official coordinates: promotion only fills a null point and never overwrites. The retained official basis for this same parcel stands (lat 35.37479, lng -85.005115, Hamilton official parcel geometry, Live_Parcels layer, centroid of the official parcel ring, confidence official, audit row `subject_coordinates_from_official_geometry`).
- Card 103 verified after the run: lat 35.37479, lng -85.005115 — unchanged and official. No coordinates fabricated anywhere in this pass.
- No Hamilton production hard-coding: `grep -niE "hamilton|mapsdev|023 003\.02|2739"` over non-test `src/landos` returns only explanatory comments and the generic county FIPS reference row (47065). The only Hamilton fixtures are in test files (`arcgis-parcel-clause-cascade.test.ts` and pre-existing suites).
- Tests rerun this pass: `npx vitest run official-parcel-gis arcgis-adapter arcgis-parcel-clause-cascade arcgis-service-discovery` → 97 tests / 5 files pass (includes "stops context probing at the reserved ceiling and leaves the query clock usable" and the discovery/query budget-split tests). No source change was made in this pass, so typecheck and production build from the previous pass remain valid.
- Git: 60 modified/untracked paths; GIS path files touched across the sprint are `arcgis-adapter.ts`, `arcgis-service-discovery.ts`, `gis-escalation.ts`, `official-parcel-gis-run.ts` plus the untracked tests `arcgis-parcel-clause-cascade.test.ts` and `official-parcel-gis-discovered-seeds.test.ts`. Nothing committed or pushed. Lake Butler (Deal 115), Deal 89/Fairview and all business deal data untouched.

Final sprint status: Lake Butler acceptance complete; Fairview unchanged verification complete; Hamilton QA coordinates official and unchanged; Hamilton QA manufactured posture MARGINAL; discovered-service reuse proven; adapter clause cascade fixed and tested; run-level query reserve proven end to end — the Hamilton QA parcel now matches automatically on the discovered county service. Hamilton GIS closure COMPLETE. No commit, no push, Stage 6 not begun.

## Operational closure (2026-09-04, replaces Stage 6)

- Release gate `npm run landos:deal-card:release`: PASS, 5/5 consecutive cases in clean isolated QA storage, operating database unchanged, 20-item capability ledger per case, clarification-resume case, write-free read proof, live currentness regression for Deals 90/89/128 (report `.runtime/landos/deal-card-release/deal-card-release-2026-09-04T23-06-45-152Z-e3f6a093.md`).
- Shared-root repairs: rerun snapshot keeps its subject stamp (`property-intelligence-snapshot.ts`); alias-produced retained read adopted by the canonical card (`canonical-deal-family.ts` + `deal-intelligence-run.ts`); APN answer rematches the waiting address-only card (`canonical-deal-resolution.ts`); parcel scope answers on a fresh store (`deal-parcel-scope-view.ts` + `property-research-store.ts`); provider panel acreage never displaces an officially confirmed acreage (`subject-identity-reconciliation.ts`); live standalone Property Resolution keeps its lock against a full-run launch (`routes.ts`); driver reads and page-scope calls honour the caller deadline (`browser-session.ts`); government-records read states its limitation before identity; county label no longer doubles "County"; Redfin exhausted-routes note keeps the page-by-page read; archived alias routes visibly resolve to the canonical card (`AcquisitionWorkspaceV2.tsx`).
- Live proof: Deals 90 (iv:151:v12#ac:1.5), 89 (iv:88:v7#ac:51.11), 128 (iv:153:v2#ac:43.7) render current Property Intelligence, Stage 3/5 artifacts and Deal Brain; browser scenario `operational-closure` PASS 67/67; route QA 128 and 89 PASS; hard refresh GET-only; console clean.
- Deed lanes (`POST browser-intel deep_record`) for 90/128/89 now terminate in under four minutes (previously hung past 30 minutes): LandPortal owner/APN/acreage retrieved; official county sources located; every county attempt `attempted_inconclusive` (destination reached, no subject fact extracted); no recorder instrument or deed image retrieved; no paid or login wall recorded.
- No commit, no push. Stage 6 not begun.

## Operational closure, second pass (2026-09-05, uncommitted)

Corrections on top of the first closure pass, all with focused regression tests:

- Williamson County parcel layer reconciled as a false identity conflict because its `county` field carries the state county number ("094"), not a name. `compareCounty` treats a numeric code as not comparable; Card 128 now adopts the official parcel centroid (35.949989, -87.154081) as a provisional match with the roll's acreage gap noted.
- Deal 90's canonical research record refused every provider result ("different canonical property") because the family read returned alias card 96's record and the merge treated it as a foreign card. `adoptFamilyResearchRecord` re-keys a same-family record onto the card the result was produced for; a same-card record keyed to a superseded APN is rebased and its old facts retired.
- Acreage: the identity version is not an acreage authority (Card 128's version carried the listing's 52.18 MLS acres). Reconciliation now defers to the shared governing acreage basis, and an unverified card's own figure ranks as provider data, so Card 128 returned to 43.7 (LandPortal parcel record) and Deal 89 keeps its assessed 51.11.
- Deal 90 zoning: Bradford County Official Zoning Atlas (updated April 2026, county Box share) read at 400% places River Oak Plantation in AGRICULTURAL-2; recorded as public-record outcome 19. Deed OR 1124/39 retrieved and read (outcome 15). Williamson deeds (Deal 89 DB 9433/325, Card 128 DB 9907/871) sit behind the Register's paid Professional Access portal (outcomes 17, 18).
- Deal Brain: Deal 89 decision 1422 written once for the $2,882,000 change; an identical rerun wrote none (count stays 4). Deal 90 decision 1489 after retention repaired (7 qualifying Land Home sales).
- Zillow: the managed persistent session is currently challenged (PerimeterX Press & Hold). Clearing it is the one operator pause; the profile lives at `C:\Users\tbutt\.landos-chrome` (managed browser is always offscreen).

Deferred (not built, with reproduction): public-record zoning outcomes are not consumed by the land-use / Development Path lane (Deal 90 and Card 128 still read "district not established"); the retrieved deed's roadway ingress/egress easement is not projected into the access evidence ladder (no `verified_legal` producer reads government-record findings); Card 128's acreage discrepancy (43.7 LandPortal / 50.8 assessor / 52.18 MLS / 33.59 county polygon) awaits operator acreage acceptance.

## Operational closure, third pass (2026-09-05, uncommitted)

- Retained official public-record zoning outcomes now enter the current-zoning race (`zoningDeterminationsFromPublicRecords`, lane `retained_public_record`): Deal 90 reads AGRICULTURAL-2 from the Bradford County Official Zoning Atlas outcome and Card 128 reads RS40 from the Fairview zoning-layer outcome; Development Path and Deal Brain refreshed once each (decisions 1518 and 1507).
- The retrieved Bradford deed (OR 1124/39, Instrument 2005189911) now asserts verified-legal access from its own ingress/egress text through the canonical research store (`recorded-instrument-access.ts`, lane `government_records`); Deal 90 reads legal access established. Its River Oak restrictions are carried as a `record.encumbrances` record fact, a Deal Brain risk row, and a material dimension.
- Card 128 acreage: accepted 50.8 (the official Williamson assessment record) through the operator-acceptance path from the controlled fixture (`landos-accept-qa-fixture-acreage.ts`, identity v3 #154, evidence 6035); the fixture now states 50.8; the extent reconciliation keeps 52.18 (intake) and 43.7 (provider) as superseded history.
- Zillow: the operator cleared Press & Hold in a visible window on the LandOS profile; the sold board loaded once, then Zillow re-challenged within minutes. One retry ran on the restored managed browser. No challenge was defeated.

## Operational closure, fourth pass (2026-09-05, uncommitted)

- Remote browser fallback: the existing Browserbase adapter (`remote-browser.ts`, persistent context, reconnection, cleanup, config-pattern credentials) gained an operator live-view URL (`liveViewUrl` from the provider's fullscreen debugger). The Zillow lane already prefers the remote session when configured, and remote results flow through the existing `fetchZillowLandComps` extraction/normalization/dedup path. Proven by an injected integration fixture (`remote-zillow-integration.test.ts`): sold, active and manufactured boards on one persistent remote session; a challenged board reports blocked with no fabricated comps. Provider challenge-solving and stealth stay OFF.
- Provisioning (the one external blocker): Browserbase, env `LANDOS_REMOTE_BROWSER_PROVIDER=browserbase`, `BROWSERBASE_API_KEY`, `BROWSERBASE_PROJECT_ID`. Verified pricing 2026-09-05: Free $0/mo (3 concurrent, 1 browser hour, persistent contexts, 15 min/session), Developer $20/mo (25 concurrent, 100 browser hours) recommended. Unverified live until credentials are set and a real remote session retrieves the three Zillow boards without a challenge.
- Development Path test fixed: an established manufactured-housing permission (by-right or special-exception) now yields a conditional land-home path even before the market screen; the focused suite is green.
- Deal 89 governing valuation restored to the accepted $3,113,000 (decision 1527 current) by operator-excluding the rerun-admitted comps 1165 and 1004 (retained as evidence, restorable, with Tyler's reason). Decision 1422 and comp 1165 preserved as history; nothing deleted.
- Token hygiene: `scrubSecretsFromText` scrubs every browser-QA and release-gate report before write; 53 legacy runtime artifacts pattern-redacted (no `.env` read); no non-redacted credential value remains anywhere, none in the git diff; `.env` unmodified (mtime 2026-09-02).

## Zillow live demonstration — accepted external limitation (2026-09-05)

Per operator revision: Zillow is an ADDITIVE best-effort provider and does not block an otherwise complete valuation, manufactured-home screen, or Deal Brain refresh when the other approved providers supply sufficient qualified evidence.

- Live three-board Zillow demonstration through one persistent session FAILED: the local persistent session was operator-verified once and re-challenged (PerimeterX Press & Hold); one managed retry was challenged again. Browserbase remote provisioning was declined by the operator. Recorded as an accepted external limitation; no challenge was ever defeated.
- Not blocked, because the other providers carried the work: Deal 90 Non-LandPortal FMV $68,000 and Combined $52,000 from six Redfin closed sales; Deal 90 manufactured screen qualified (7 sales above $200,000, top $290,000) from Redfin plus retained Zillow records; Deal 89 governing $3,113,000 from LandPortal/LandWatch/Redfin; every standing Deal Brain decision refreshed.
- Preserved and available for when Zillow is reachable: the local persistent session, the operator Resume-Zillow action, the Browserbase remote adapter (persistent context, reconnection, cleanup, operator live-view) with challenge-solving off, challenge detection that reports blocked rather than fabricating, and the one canonical evidence path (remote results flow through the same extraction, dedup, registry, valuation, map, manufactured and Deal Brain lifecycle).
