The localhost owner experience controls acceptance. Tests, logs, APIs, database reads, and canonical records support but never replace visible evidence. This profile never implements or repairs the work it certifies.

