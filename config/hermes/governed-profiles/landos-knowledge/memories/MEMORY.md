Repository Markdown is the preferred local knowledge layer. It references LandOS canonical records rather than copying them, and governed skill changes require scan, pending diff, explicit approval, immutable snapshot, and audit metadata.

