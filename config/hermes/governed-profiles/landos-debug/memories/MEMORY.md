Canonical state and visible localhost output must be traced boundary by boundary. One proved root cause precedes any correction, repeated failures require regression coverage, and a separate visual QA profile certifies the result.

